/**
 * _abs - computes the absolute value of an integer
 * @num: the inr=teger to be computed
 * Return: The absolute value
 */
int _abs(int num)
{
	if (num < 0)
		return (-num);
	else
		return (num);
}
